# COMP484-HW-9
the easiest part of the project was the math section and using toFixed(2), because once the variables were set up, the maths was simple. The hardest part was definitely formatting the date correctly. I didn't realise that JavaScript starts counting months at 0, so I had to add 1 to get the right month, and then write extra logic to make sure single digits like "3" showed up as "03" to match the MM/DD/YYYY requirement.I learned that the Date object needs a lot of manual tweaking to look right on a page. I also learned how to use the Number object to validate data, like checking if a string actually converted into a number or ended up as NaN.
